on:
  push:
    branches:
        - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Build and push http-backend image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: dockerfiles/dockerfile.httpbackend
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/veda-ai-backend:prod

      - name: Deploy to Production Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            sudo docker pull ${{ secrets.DOCKER_HUB_USERNAME }}/veda-ai-backend:prod 
            # Use the EXACT name you defined in the --name flag below
            sudo docker stop veda-ai-backend || true
            sudo docker rm veda-ai-backend || true
            sudo docker run -d -p 3001:3001 --name veda-ai-backend \
            -e JWT_SECRET="${{secrets.JWT_SECRET}}" -e ADMIN_SECRET="${{secrets.ADMIN_SECRET}}" -e MONGO_URL="${{secrets.MONGO_URL}}" \
            -e REDIS_URL="${{secrets.REDIS_URL}}" -e GROQ_API_KEY="${{secrets.GROQ_API_KEY}}" \
            ${{ secrets.DOCKER_HUB_USERNAME }}/veda-ai-backend:prod


    