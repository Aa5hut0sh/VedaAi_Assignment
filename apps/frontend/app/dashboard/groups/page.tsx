import PlaceholderState from "@/components/shared/PlaceholderState";

export default function GroupsPage() {
  return (
    <PlaceholderState
      title="Groups are not wired yet"
      description="This route is now safe to visit and can be filled with a basic classroom list, invite flow, and group cards later without breaking the sidebar."
      ctaHref="/dashboard/assignments"
      ctaLabel="Go to Assignments"
    />
  );
}
