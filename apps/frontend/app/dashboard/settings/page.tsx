import PlaceholderState from "@/components/shared/PlaceholderState";

export default function SettingsPage() {
  return (
    <PlaceholderState
      title="Settings page placeholder"
      description="Profile, school, and notification settings can land here once the backend routes are ready."
      ctaHref="/dashboard"
      ctaLabel="Back to Dashboard"
    />
  );
}
