import PlaceholderState from "@/components/shared/PlaceholderState";

export default function LibraryPage() {
  return (
    <PlaceholderState
      title="Library is coming next"
      description="Use this area later for saved papers, PDFs, and reusable templates. For now it stays visually consistent with the app shell."
      ctaHref="/dashboard/create"
      ctaLabel="Create Assignment"
    />
  );
}
